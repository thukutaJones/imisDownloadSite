<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="icon" href="<%= icon %>" type="image/png" />
    <link href="/css/tailwind.css" rel="stylesheet" />
    <style>
      .scroll-container::-webkit-scrollbar {
        display: none;
        scrollbar-width: none;
        -ms-overflow-style: none;
      }
    </style>
  </head>
  <body class="bg-white">
    <div class="container mx-auto p-4">
      <div class="flex flex-row gap-4">
        <img
          src="<%= icon %>" 
          height="5px"
          width="60px"
        />
        <div>
          <h1 class="text-gray-600 font-bold text-2xl"><%= title %></h1>
          <h1 class="text-primary">Moth Inc@2024</h1>
        </div>
      </div>
      <a href="<%= downloadLink %>">
        <button
          class="items-center justify-center bg-blue-600 w-full p-2 mt-6 rounded-2xl"
        >
          <p class="text-gray-200">Download</p>
        </button>
      </a>
      <div
        class="flex flex-row mt-6 overflow-auto whitespace-normal gap-2 scroll-container"
      >
        <% for (let i = 0; i < appScreenshots.length; i++) { %>
        <img
          src="<%= appScreenshots[i] %>"
          class="h-56 rounded-lg border border-gray-200"
        />
        <% } %>
      </div>
      <h1 class="text-gray-500 text-2xl mt-28 font-bold">About This App:</h1>
      <p class="text-sm text-gray-800"><%= appDescription %></p>
    </div>
  </body>
</html>
